:'
1)A flag docker run [--interactive] ou [-i]:
    -> Mantenha STDIN aberto, mesmo que não esteja conectado
    -> Permite manter a entrada padrão |shell| aberta
    -> Quando usamos [--interactive], não precisamos usar o [--attch], ele já vai fazer isso por padrão além de manter a entrada ativa/aberta.
'